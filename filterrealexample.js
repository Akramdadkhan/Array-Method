const products =[
    { title : "ABC",size:7,color:"Black"},
    { title : "gHJ",size:6,color:"White"},
    { title : "sST",size:8,color:"Green"},
    { title : "TYE",size:3,color:"White"},
    { title : "wIW",size:7,color:"Black"},
    { title : "IEO",size:4,color:"Green"},
    { title : "wIW",size:6,color:"Black"},
];

// const sorted= products.sort((a,b)=>a.title.toLowerCase() > b.title.toLowerCase() ? 5 : -5);

// console.log(sorted)
// Jab itte bade object rahe to array method ko key se bulana padega...
// const filterd = products.filter((item)=>item.color === "Black");
// console.log(filterd)

// const filterd = products.filter((item)=>item.size < 8 && item.color==="White" && item.title );
// console.log(filterd)

// const sorted = products.sort((a,b)=>a.title.toUpperCase() -b.title.toUpperCase() ? 1 : -1);
// console.log(sorted)
